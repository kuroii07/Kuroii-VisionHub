# VisionHub 参考图迁移包

`reference-packs/` 用来记录一次性参考图迁移说明，不保存图片本体。

- 生成图片 ZIP 不进入 `main` 源码分支。
- 如需同步公司机器上的参考图，使用独立 GitHub 分支或 Release Asset。
- 回家后解压 ZIP，在 VisionHub Studio 的作品画廊或灵感中心图片收藏中使用“导入本地图片 / 批量导入文件夹”。
- ZIP 只应包含图片和非敏感 manifest；不要包含 API Key、raw 响应、AppData 配置或系统凭据。
