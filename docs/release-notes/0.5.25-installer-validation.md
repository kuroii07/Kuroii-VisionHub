# Kuroii VisionHub 0.5.25 Windows Installer Validation

Date: 2026-07-15

## Scope

Validate the published `0.5.25` NSIS and MSI artifacts without clearing AppData, gallery data, settings, caches, or credentials. Restore the machine to an installed `0.5.25` state after testing.

## Safety preparation

- Stopped all Kuroii VisionHub processes before installation and uninstall operations.
- Archived `%APPDATA%\studio.visionhub.app` and `%LOCALAPPDATA%\studio.visionhub.app` under the ignored `backups/pre-installer-validation-*` directory.
- Recorded SHA256 values for 22 persistent-data candidates before testing.
- Left the existing legacy `VisionHub Studio 0.5.0` installation untouched.

## NSIS results

- Silent install command: `Kuroii VisionHub_0.5.25_x64-setup.exe /S`
- Install exit code: `0`
- Install scope: current user
- Install directory: `%LOCALAPPDATA%\Kuroii VisionHub`
- Registered version: `0.5.25`
- Launch smoke: responsive after 12 seconds, window title `Kuroii VisionHub`, file/product version `0.5.25`
- Start Menu and Desktop shortcuts: created
- Silent uninstall exit code: `0`
- Install directory and uninstall entry after uninstall: removed
- Roaming and Local AppData after uninstall: retained
- Baseline persistent files missing: `0`
- Roaming persistence hash changes: `0`

## MSI results

- MSI metadata: `ALLUSERS=1`, product version `0.5.25`, product code `{CE26ABA9-2CA7-4AFE-926B-D94E743DC35D}`
- Default non-admin silent install: returned `1603` with Windows Installer `Error 1925`, confirming administrator privileges are required for the default all-users path.
- Current-user override:

```powershell
msiexec /i "Kuroii VisionHub_0.5.25_x64_en-US.msi" ALLUSERS=2 MSIINSTALLPERUSER=1 /qn /norestart
```

- Current-user override install exit code: `0`
- Install directory: `%LOCALAPPDATA%\Kuroii VisionHub`
- Launch smoke: responsive after 12 seconds, window title `Kuroii VisionHub`, file/product version `0.5.25`
- Current-user override uninstall exit code: `0`
- Install directory and MSI product registration after uninstall: removed
- Roaming and Local AppData after uninstall: retained
- Baseline persistent files missing: `0`
- Roaming persistence hash changes: `0`

## Final machine state

- Reinstalled the NSIS `0.5.25` build successfully.
- Final installed application launched and remained responsive.
- Final AppData check: `0` baseline files missing and `0` Roaming persistence hash changes.
- Existing legacy `VisionHub Studio 0.5.0` installation remains present and was not modified.

## Remaining manual checks

- Interactive installer language and layout.
- Windows SmartScreen / unknown-publisher prompt.
- Elevated all-users MSI install and uninstall.
- Upgrade from an installed previous Kuroii VisionHub release.
- Fresh-profile or second-computer migration and settings-backup restore.
