# Dependencies
node_modules/
.npm-cache/

# Frontend build output
dist/
.vite-cache/
output/
outputs/
.codex-browser-profile/

# Tauri / Rust generated output
src-tauri/target/
src-tauri/gen/

# Local runtime reports
# These are generated when launching or validating locally and differ per machine.
docs/run-reports/
backups/

# Local / migration-only reference packs
# Keep generated images out of the main source branch. If a one-off transfer is
# needed, push the archive to a dedicated reference-pack branch or release asset.
reference-packs/*.zip

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env / secrets
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
